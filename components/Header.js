import React from 'react';
import headStyles from '../styles/header.module.css';

const Header = () => {
  return (
    <head className={headStyles.head}>
      Next cohort kicks off on May 9th. Seats are booking fast. Apply today!
    </head>
  );
};

export default Header;
